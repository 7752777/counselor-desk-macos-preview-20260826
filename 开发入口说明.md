# 辅导员工作台唯一开发入口

以后所有开发、测试和构建只在以下文件夹进行：

`F:\CounselorDesk\counselor-desk-development`

当前分支：`codex/ai-upgrade`

## 常用命令

```powershell
Set-Location 'F:\CounselorDesk\counselor-desk-development'
pnpm lint
pnpm test:cwb-business
pnpm test:cwb-ai
pnpm test:cwb-employment
pnpm run check:secrets
pnpm desktop:dev
pnpm desktop:build:win
```

源码入口是根目录 `index.html`，Electron 主进程入口是 `desktop/main.cjs`，唯一桌面打包配置是根目录 `electron-builder.yml`。

不要在 `F:\CounselorDesk\_archive-non-development-20260817` 内开发，也不要把归档目录中的旧源码、候选包、参考图片或用户数据重新当作开发基线。

当前正式发布：v4.8.5。正式代码冻结由 [`v4.8.5` Tag](https://github.com/7752777/counselor-desk/releases/tag/v4.8.5) 指向本次维护发布提交；v4.8.3、v4.8.2、v4.8.1、v4.8.0 的 Tag、Release 和历史资产保持不变。仓库为私有，当前维护分支为 `codex/ai-upgrade`；后续代码修改应以新维护提交或维护版本发布，不移动历史 Tag，不用本机生成的 HTML 替代跨平台 Release 产物。

当前工作区候选：v4.9.0。该候选先完成商业授权、AI 守卫、签名更新和发码服务契约；生产签名、公证、支付、KMS、CDN 与正式 Release 仍需独立部署和验收。
